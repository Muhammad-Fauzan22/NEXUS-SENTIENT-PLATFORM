# .npmrc
# File konfigurasi npm untuk proyek NEXUS-SENTIENT-PLATFORM.
# Menyimpan pengaturan registry, cache, dan konfigurasi lainnya untuk konsistensi lingkungan.

# --- Registry ---
# Gunakan registry npmjs.org sebagai default
# registry=https://registry.npmjs.org/

# --- Cache ---
# Tentukan lokasi cache npm (opsional, untuk menghindari masalah permission di sistem tertentu)
# cache=.npm-cache

# --- Engine Strict ---
# Paksa npm untuk memeriksa versi Node.js yang diperlukan oleh paket (direkomendasikan untuk konsistensi)
engine-strict=true

# --- Save Exact ---
# Simpan versi paket secara eksak (bukan range) di package.json untuk menghindari perbedaan versi
# di lingkungan yang berbeda. Ini meningkatkan stabilitas dan reproduksibilitas.
save-exact=true

# --- Audit ---
# Nonaktifkan audit keamanan saat install untuk mempercepat proses (aktifkan jika diperlukan)
# audit=false

# --- Fund ---
# Nonaktifkan pesan "fund" saat install untuk mengurangi noise di log (aktifkan jika diperlukan)
# fund=false

# --- Progress ---
# Nonaktifkan progress bar untuk instalasi yang lebih cepat di terminal modern
# progress=false

# --- Legacy Peer Deps ---
# Nonaktifkan pemeriksaan peer dependencies yang ketat (jika mengalami konflik versi paket)
# Hanya digunakan jika diperlukan untuk mengatasi masalah instalasi.
# legacy-peer-deps=true